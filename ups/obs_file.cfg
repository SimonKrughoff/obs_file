# -*- python -*-

import lsst.sconsUtils

dependencies = {
    "required": ["afw", "obs_base"],
    "buildRequired": ["swig"],
}

config = lsst.sconsUtils.Configuration(
    __file__,
    hasDoxygenInclude=False,
    hasSwigFiles=False,
)
